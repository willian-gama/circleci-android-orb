description: Determine if the .build gradle files have been updated to enforce download dependencies again.

parameters:
  test_command:
    type: string
    description: UI test command

steps:
  - android/run-tests:
      max-tries: 3
      no-output-timeout: 5m
      test-command: << parameters.test_command >>