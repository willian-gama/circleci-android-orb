description: Restore gradle cache to speed up the build process.

steps:
  - run:
      name: Prepare android cache args
      command: <<include(scripts/prepare_android_cache_args.sh)>>
  - android/restore-gradle-cache:
      find-args: $ANDROID_CACHE_ARGS