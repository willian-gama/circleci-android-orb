description: Docker client executor to run jobs.

parameters:
  resource_class:
    type: enum
    description: Configure the executor resource class
    enum: ["medium", "large"]

machine:
  image: ubuntu-2004:202101-01
  resource_class: << parameters.resource_class >>