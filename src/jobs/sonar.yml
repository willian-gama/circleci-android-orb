description: Upload reports to Sonar cloud.

executor:
  name: android_executor
  resource_class: large

steps:
  - load_workspace
  - restore_gradle_cache
  - android/restore-build-cache
  - run:
      name: Analyze on SonarCloud
      command: <<include(scripts/upload_to_sonar.sh)>>