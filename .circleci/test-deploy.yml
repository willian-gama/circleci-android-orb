version: 2.1
orbs:
  # Your orb will be automatically injected here during the pipeline.
  # Reference your orb's jobs and commands below as they will exist when built.
  orb-tools: circleci/orb-tools@12.0
  # The orb definition is intentionally not included here. It will be injected into the pipeline.
  <orb-name>: {}

# Use this tag to ensure test jobs always run,
# even though the downstream publish job will only run on release tags.
filters: &filters
  tags:
    only: /.*/

# Filter for release tags.
release-filters: &release-filters
  branches:
    ignore: /.*/
  tags:
    only: /^v[0-9]+\.[0-9]+\.[0-9]+$/

checkout: &checkout
  - run:
      name: Checkout
      command: git clone -b "$CIRCLE_BRANCH" << pipeline.trigger_parameters.github_app.repo_url >> . # trigger_parameters.github_app.repo_url is available in Pipeline values - https://circleci.com/docs/variables/#pipeline-values

jobs:
  # Create jobs to test the commands of your orbs.
  # You may want to add additional validation steps to ensure the commands are working as expected.
  command-test:
    executor: android_executor
    steps:
      - *checkout
      # - willian-gama/setup
      # - willian-gama/code_linting:
      #     build_variant: debug
      # - willian-gama/unit_tests:
      #     run_screenshot_tests: true

workflows:
  test-deploy:
    jobs:
      # Make sure to include "filters: *filters" in every test job you want to run as part of your deployment.
      # Test your orb's commands in a custom job and test your orb's jobs directly as a part of this workflow.
      - command-test
      # The orb must be re-packed for publishing, and saved to the workspace.
      - orb-tools/pack:
          pre-steps:
            - *checkout
          checkout: false
          filters: *release-filters
      - orb-tools/publish:
          orb_name: willian-gama/android-ci
          vcs_type: github
          pub_type: production
          # Ensure this job requires all test jobs and the pack job.
          requires:
            - orb-tools/pack
            - command-test
          context: orb-publishing
          filters: *release-filters
